/**
 * Debug UI overlay module
 * Responsibilities:
 * - Creates and manages the debug overlay in top-left corner
 * - Displays live numeric values of all audio metrics
 * - Updates display each frame with current metric values
 * - Provides visual feedback for debugging metric calculations
 * - Minimal HTML/CSS-based UI (no frameworks)
 */

