/**
 * Main application entry point and render loop
 * Responsibilities:
 * - Initializes the application
 * - Coordinates between audio analyzer and WebGL renderer
 * - Manages the animation frame loop
 * - Updates shader uniforms with audio metrics each frame
 * - Handles audio input button click and getUserMedia setup
 */

